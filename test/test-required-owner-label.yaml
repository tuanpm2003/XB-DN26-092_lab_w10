apiVersion: v1
kind: Pod
metadata:
  name: require-owner-label-test
  namespace: demo
spec:
  containers:
    - name: nginx
      image: nginx:1.27.5
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
      resources:
        requests:
          cpu: "100m"
          memory: "128Mi"
        limits:
          cpu: "200m"
          memory: "256Mi"